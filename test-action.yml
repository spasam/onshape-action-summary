name: 'test-action-summary'
description: 'Test summary wf'
inputs:
outputs:

runs:
  steps:
    - name: test-summary
      uses: onshape/onshape-action-summary@onshape-action-summary
      shell: bash
      run: |
        echo "Running..."

